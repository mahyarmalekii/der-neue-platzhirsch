[build]
  command = "bun run build || npm run build"
  publish = "dist"

[build.environment]
  NITRO_PRESET = "netlify"
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
